apply plugin: 'java'

sourceCompatibility = 1.6
version = '0.1'

repositories {
    mavenCentral()
}

dependencies {
    compile group: 'org.hibernate', name: 'hibernate-core', version: '3.6.10'
    testCompile group: 'junit', name: 'junit', version: '4.+'
}
